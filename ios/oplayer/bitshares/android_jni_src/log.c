#include "log.h"
// #include "crashlytics.h"

void cls_log(const char* message)
{
    // static crashlytics_context_t* context = 0;

    // if (!message){
    //     return;
    // }
    // if (!context){
    //     context = crashlytics_init();
    //     LOGD("jni crashlytics_init: %p\n", context);  
    //     if (context){
    //         context->log(context, "crashlytics_init");
    //     }
    // }
    // if (!context){
    //     return;
    // }
    // context->log(context, message);
}